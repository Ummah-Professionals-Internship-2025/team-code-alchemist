import { Box, Typography } from "@mui/material";

const Contacts = () => {
  return (
    <Box m="20px">
      <Typography variant="h2" color="white">Contacts Page</Typography>
    </Box>
  );
};

export default Contacts;
