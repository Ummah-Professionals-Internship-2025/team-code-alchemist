import { Box, Typography } from "@mui/material";

const Form = () => {
  return (
    <Box m="20px">
      <Typography variant="h2" color="white">Form Page</Typography>
    </Box>
  );
};

export default Form;
